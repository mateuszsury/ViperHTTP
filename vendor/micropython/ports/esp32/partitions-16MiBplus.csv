# This partition table is for devices with 16MiB or more of flash and OTA support.
# The first ~7MiB is used for bootloader, nvs, phy_init and firmware slots.
# The remaining flash is for the user filesystem(s).

# Notes: the offset of the partition table itself is set in
# $IDF_PATH/components/partition_table/Kconfig.projbuild.
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x4000,
otadata,  data, ota,     0xd000,   0x2000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x380000,
ota_0,    app,  ota_0,   0x390000, 0x380000,
vfs,      data, fat,     0x710000, 0x8F0000,
